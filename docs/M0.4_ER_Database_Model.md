# M0.4 — Modelo ER do Banco Operacional (PostgreSQL v5.4)

## Diagrama Entidade-Relacionamento (Mermaid)

```mermaid
erDiagram
    TENANT {
        uuid id PK
        string slug UK
        string name
        string status
        timestamp created_at
    }

    USER {
        uuid id PK
        uuid tenant_id PK, FK
        string email UK
        string name
        string mfa_status
        timestamp last_login
        int version
    }

    ROLE {
        uuid id PK
        uuid tenant_id FK "Nullable: Role global"
        string name
        string description
    }

    RESOURCE_TYPE {
        string name PK
        string description
    }

    POLICY {
        uuid id PK
        uuid role_id FK
        uuid tenant_id FK
        string resource_type_name FK "Ref: RESOURCE_TYPE"
        string action
        string effect
    }

    USER_ROLE {
        uuid tenant_id PK, FK
        uuid user_id PK, FK
        uuid role_id PK, FK
    }

    INCIDENT {
        uuid id PK
        uuid tenant_id PK, FK
        string status
        string severity
        int risk_score
        uuid owner_id FK
        uuid asset_id FK
        jsonb ai_context
        int version
        timestamp created_at
        timestamp updated_at
    }

    INCIDENT_TIMELINE {
        uuid id PK
        uuid tenant_id PK, FK
        uuid incident_id FK
        string event_type
        string correlation_key FK "Ref: PROCESSED_EVENTS (tenant_id, correlation_key)"
        jsonb payload_summary
        timestamp occurred_at
    }

    ASSET {
        uuid id PK
        uuid tenant_id PK, FK
        string hostname
        string primary_ip
        string criticality
        string status
        timestamp last_seen
    }

    PROCESSED_EVENTS {
        string correlation_key PK
        uuid tenant_id PK, FK
        string event_type
        timestamp processed_at
        timestamp expires_at
    }

    TENANT ||--o{ USER : "possui"
    TENANT ||--o{ ASSET : "gerencia"
    TENANT ||--o{ INCIDENT : "registra"
    TENANT ||--o{ ROLE : "define"
    TENANT ||--o{ POLICY : "define"

    ROLE ||--o{ USER_ROLE : "atribuído a"
    ROLE ||--o{ POLICY : "define permissões via"
    RESOURCE_TYPE ||--o{ POLICY : "tipa"
    USER ||--o{ USER_ROLE : "possui"
    INCIDENT ||--|{ INCIDENT_TIMELINE : "possui histórico"

    ASSET ||--o{ INCIDENT : "sofre"
    USER ||--o{ INCIDENT : "dono"
    PROCESSED_EVENTS ||--o{ INCIDENT_TIMELINE : "originou (dedupe)"
```

---

## 🔒 Garantia Cross-Tenant (Compound Foreign Keys)

Para impedir *Cross-Tenant Corruption* (ex: incidente do Tenant A apontar para um asset do Tenant B), todas as relações utilizam Foreign Keys compostas contendo `tenant_id`:

- `FOREIGN KEY (tenant_id, asset_id) REFERENCES ASSET(tenant_id, id)`
- `FOREIGN KEY (tenant_id, owner_id) REFERENCES USER(tenant_id, id)`
- `FOREIGN KEY (tenant_id, incident_id) REFERENCES INCIDENT(tenant_id, id)`
- `FOREIGN KEY (tenant_id, correlation_key) REFERENCES PROCESSED_EVENTS(tenant_id, correlation_key)`
